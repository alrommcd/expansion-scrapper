# Decisions: HouseEazy Expansion Intelligence Agent
<!-- Append-only. One line per entry. Never delete. -->
<!-- Format: [DATE] DECISION or LESSON: what and why. -->

[2026-07-03] Stack: Python + Playwright + Gemini API + SQLite + CLI/JSON output. Chosen because scraping + LLM classification points to Python, and v1 is a local demo with no hosting need yet.
[2026-07-03] Society scoring locked to 5 signals: price-band fit, resale liquidity, listing density, RERA/title cleanliness, possession status. Weighted sum, confirmed with user in Plan Mode.
[2026-07-03] Broker scoring locked to 4 signals: listing volume in corridor, distinct-society coverage breadth, Maps/JustDial rating+reviews, listing recency. Weighted sum, confirmed with user in Plan Mode.
[2026-07-03] Absentee/NRI scoring locked to 4 signals (agent-managed signal dropped as too noisy): NRI/owner-abroad mention, POA mention, owner-not-local phrasing, long-tenanted/re-listed. Weighted density score, explicitly labeled a low-confidence heuristic in all output, never a factual claim. Confirmed with user in Plan Mode.
[2026-07-03] Corridor gates are pass/fail thresholds (a corridor must clear all 5 to be eligible), then survivors are ranked by an equal-weighted composite of the 5 normalized gate scores. This is an assumption, not explicitly specified by the user, flagged inline in code comments. Corridor metric definitions themselves are locked and must not be re-derived.
[2026-07-03] Milestone 1 (corridor engine) runs against seed/placeholder Pune corridor metrics, not live scraped data, to prove the scoring logic and city-agnostic architecture first. Real data hookup is Milestone 2+ (scraper layer). This must not be confused with a live validated result.
[2026-07-03] Switched from `google-generativeai` to `google-genai` for Gemini API access. The former package hit end-of-life during setup (deprecation warning on import); the latter is Google's current supported SDK.
[2026-07-03] Scraping source recon: 99acres blocked outright (403, Akamai bot protection) on first page load, marked inactive in city config. NoBroker loads but renders listings via internal JSON APIs (client-side), deferred to v1.5 rather than reverse-engineering the filtered endpoint now. MagicBricks is server-rendered and not blocked, real listing cards present on first load (society name, price, BHK, status, transaction type, owner name, RERA number in description). Chosen as the sole v1 scraping source.
[2026-07-03] Lesson: don't guess a listing site's URL slug format and assume 404 means blocked. MagicBricks' locality URL is lowercase (`flats-in-hinjewadi-pune-for-sale-pppfs`); the capitalized guess 404'd while the lowercase one returned 200. Always confirm with an actual request before concluding a source is inaccessible.
[2026-07-03] Lesson: gemini-2.5-flash's free tier caps at 20 requests/DAY (not per-minute), hit and exhausted during classification testing on 30 Hinjewadi listings. Switched the classifier to gemini-2.5-flash-lite, which carries a separate, higher free-tier daily quota. Also batches ~10 listings per Gemini call (not one call per listing) and added jittered exponential backoff on 429s. User declined enabling billing for v1.
[2026-07-03] Design: Gemini is only asked for genuinely unstructured judgment (the 4 absentee/NRI flags + society-name cleanup). Price, area, BHK, listing_type, possession_status, RERA-mention, and posting recency are all parsed deterministically in classify/parsing.py via regex, since MagicBricks presents them as near-structured labeled fields and CLAUDE.md bars the LLM from ever inventing a number.
[2026-07-03] Society scoring signal proxies (documented in scoring/society_config.py): `resale_liquidity` uses listing posting recency (days_since_posted) as a stand-in for true recent-transaction-count + true days-on-market, because a listings scrape only sees active listings, not historical sales. `title_cleanliness` uses presence of the word "RERA" in the free-text description as a stand-in for a verified MahaRERA registry lookup. Both are flagged as v1 proxies pending the real comp/RERA data sources (v2 scope, not the IGR layer which is out of scope entirely for v1).
[2026-07-03] Milestone 2 verified end-to-end on 30 real Hinjewadi listings scraped from MagicBricks: scrape -> classify (Gemini flash-lite + deterministic parsing) -> entity resolution (100% match rate on named listings, real canonical society list built from this scrape) -> society fit score + absentee density -> joined ranked table. Absentee flags are all 0 in this sample; verified by keyword-grepping the raw text that no NRI/POA/tenant signal exists in this data, not a classifier failure.
[2026-07-04] User expanded absentee/NRI detection to a tiered point system (Tier 1 = 40pts, Tier 2 = 20pts, Tier 3 = 10pts; 60+ Hot, 30-59 Warm, <30 Cold). Three requested Tier 3 items were pushed back on and resolved: (1) surname-based heuristic (Patel/Shah/Mehta/etc) - declined, reconfirms the original kickoff brief's exclusion, bias-prone; (2) "known NRI-heavy society" - not implemented as an inferred/circular heuristic, instead added as an explicit user-curated list in city config (`CorridorCandidate.nri_heavy_societies`, empty until user supplies names); (3) below-market price as an NRI signal - user overrode the recommendation to skip despite it contradicting the classifier's existing "never infer NRI from price" rule; implemented as specified, flagged as a known false-positive risk the user explicitly accepted. Also declined scraping revealed phone numbers (user asked for this, but MagicBricks gates it behind a click-through specifically to prevent bulk harvesting, and capturing individual phone numbers cross-referenced with inferred NRI status conflicts with the project's own DPDP "aggregate only, no named individuals" boundary from the kickoff brief) - implemented a text-only regex check on already-scraped description/title instead.
[2026-07-05] Second live MagicBricks scrape of Hinjewadi (same corridor, 2 days later) returned an almost entirely different set of 30 active listings from the first scrape - only 2 society names overlapped. Confirms the Phase 1 pushback about entity-resolution fragility: a scrape-derived canonical society list is a moving target, not a stable reference. Merged both scrapes' observed names into one canonical list for now (23 societies); the durable fix is sourcing canonical names from MahaRERA instead, deferred past v1.
[2026-07-05] Lesson: initial `flag_investment_property_mention` prompt caused a real false positive - it matched MagicBricks' generic "buy or invest in Pune city" marketing boilerplate (present on some listings regardless of actual investment status) as if it were a genuine signal. Verified by grepping raw text: 3/30 listings matched, all boilerplate, zero genuine investment-property mentions. Fixed by explicitly excluding generic buy/invest calls-to-action in the prompt and requiring a specific claim about the property's current rental/investment status. Re-classified and re-verified: 0 false positives after the fix.
[2026-07-05] Full re-verification on 30 real Hinjewadi listings with the expanded tiered detection: zero Tier 1 or Tier 2 (explicit-text) signals fired anywhere in the sample. The only non-zero scores come from the user-mandated below-market-price override (4 listings, all "Cold" tier, 10/100+ points). No "Hot" or "Warm" leads exist in this real sample under the new logic. This is consistent with, not contradictory to, the original Phase 1 prediction that explicit absentee/NRI evidence is rare in real listing text.
[2026-07-05] User pivoted strategy after the zero-NRI-density result: active portal listings self-select for engaged owners, so absentee signal should come from staleness/vacancy, not text mining of active listings. Proposed 3 sub-layers (2A stale-listing mining, 2B society-level vacancy mapping, 2C MahaRERA cross-reference). Recon before committing: (1) 2A's "no price reduction in listing history" and "reposted multiple times" are NOT derivable from any single scrape at any depth - they require comparing snapshots over time (recurring scrape + diff), which does not exist yet. "Listing age > 90 days" IS derivable from one scrape: MagicBricks switches from relative ("Updated X ago") to absolute ("Posted: May 07, '26") phrasing once a listing is old enough, confirmed by paginating to page 20. (2) 2C's given URL (maharerait.mahaonline.gov.in) returned connection-refused from this sandboxed environment; the domain already in city config (maharera.mahaonline.gov.in) is alive and redirects to that same host, confirming it's the real site, just unreachable here (possibly network/geo-restricted, not confirmed permanent). User chose: build 2B now, defer 2A's history-dependent filters, skip 2C for now (user will independently check MahaRERA access).
[2026-07-05] Extended `parse_days_since_posted` (classify/parsing.py) to parse MagicBricks' absolute "Posted: MMM DD, 'YY" date format, not just relative phrasing. Added pagination to the MagicBricks scraper (`?page=N`, confirmed working through at least page 20) since page 1 alone is 100% same-day-fresh listings (no "oldest first" sort exists, only "Most Recent").
[2026-07-05] Layer 2B (scoring/vacancy.py) is deliberately 100% deterministic, no Gemini call: listing age, price, and society name are all near-structured fields, so there's no free text here that benefits from an LLM read. This keeps the layer fast, free, and fully auditable.
[2026-07-05] Scraped 20 pages (600 listings, page 1-20) of Hinjewadi to get a representative age distribution for Layer 2B: ages ranged 0-90 days (avg 12.7), 149 distinct societies observed. Replaced the small hand-accumulated canonical society list (23 names, built from two 30-listing samples) with the full 149-name list observed in this larger, more representative scrape, picking each society's most common raw casing variant as the display name. Coverage jumped from ~33% to ~99% of listings (556/558 with a society name matched to a canonical entry).
[2026-07-05] Lesson: raw vacancy scores ranked by nri_probability_score alone put single-listing societies at the top purely from small-sample noise (n=1 with one 90-day-old listing scores identically to a real pattern). Added a `sample_confidence` field ('low' <3 listings, 'medium' 3-9, 'high' 10+) to the output instead of silently filtering low-n societies out - keeps the data visible but flags it as unreliable to rank on.
[2026-07-05] User requested a full multi-city architecture (city input -> auto-discover corridors -> score all corridors (fit + absentee) -> drill into top 5 societies). Read the full codebase first per user instruction, corrected two misunderstandings in the request: no unified `scoring_config.py` exists (4 separate config modules by design), no `nri_societies.json` exists (it's a Python field, `CorridorCandidate.nri_heavy_societies`). Resolved 4 architecture forks with the user: (1) NoBroker for corridor-NAME discovery only, MagicBricks stays the listing scraper; (2) TWO corridor tables kept separate - the locked 5-gate Fit Score (unchanged) and a NEW Absentee/NRI Score (vacancy rate, staleness, NRI signal hit rate, below-market %), combined into a priority ranking, neither replaces the other; (3) city-wide corridor pass uses vacancy-only deterministic scoring (no Gemini, no quota risk), full Gemini NRI classification reserved for the top-5 society drill-down only; (4) deferred full FK-normalized storage, kept the current TEXT-keyed schema, added a planned `corridors_meta` table (city_id, corridor, fit_score, absentee_score, priority, last_scraped, listing_count) for Phase C.
[2026-07-05] Recon before building Phase A: verified K-RERA (rera.karnataka.gov.in) and TS-RERA (rera.telangana.gov.in) are real and reachable. Caught that `tnrera.in` (initial guess) is a parked domain ("contact the domain owner"), not the real TN-RERA site - the real one is `rera.tn.gov.in`, verified reachable with real content. Also found NoBroker's real location-autocomplete endpoint (`nobroker.in/places/api/v1/autocomplete?hint=X&city=Y`) via network capture, but it 500s on a bare direct request - it needs session cookies/context from an actual page visit, not just a URL hit. Per user decision, stubbed NoBroker discovery (`discover_corridors_via_nobroker` always returns None) rather than building the slower/more-fragile UI-driven version now.
[2026-07-05] User asked for "real well-known societies, not scraped yet" for Wakad/Kharadi/Baner/Koregaon Park/Wagholi/Katraj, to replace their placeholder seed names. Declined to recall specific building names from memory (fabrication risk - this project already caught one wrong-domain assumption same day) and instead did a real 1-page scrape per corridor (9 corridors x ~30 listings). Also replaced Pune's corridor list entirely with the user's specified 10 corridors (Hinjewadi, Wakad, Baner, Kharadi, Hadapsar, Magarpatta Road, Viman Nagar, Balewadi, Ambegaon, Undri), dropping Koregaon Park/Wagholi/Katraj (fictional gate-failure examples) - Ambegaon and Undri are real corridors that also happen to fail the gates (peripheral, far from all 3 demand anchors), so they now serve as the known-weak examples instead. Updated tests/test_corridor_engine.py accordingly. Note: bare "magarpatta" 404s on MagicBricks - the real slug is "magarpatta-road" (a distinct, broader locality from the pricier "magarpatta-city").
[2026-07-05] Phase A complete: city-agnostic corridor discovery (config/city_corridors.json seed + scraper/corridor_discovery.py) and 4 new city configs (Mumbai, Bangalore, Hyderabad, Chennai) added, registered in config/cities/__init__.py. All corridor_candidates metrics for the new cities are placeholder (never scraped), same honesty convention as Pune's originals. Hyderabad and Chennai corridor names are Claude's proposal (well-known IT-hub localities, not user-specified), approved by user since the original pivot message only covered Pune/Mumbai/Bangalore.
[2026-07-05] Phase B pre-flight (before any scraping): checked real Hinjewadi data (n=600) against the user's specified Absentee Score formula. stale_90d_pct=7.0, vacancy_pct=1.0 (text-based "vacant"/"immediate possession" mentions are rare, 0.7%/0% respectively - opposite problem from the "buy or invest" boilerplate false-positive, this one under-fires), below_market_pct=33.9% (surprisingly high, likely reflects normal BHK-size-driven price/sqft variance rather than genuine distress - using price-per-sqft not raw price, consistent with the existing Tier-3 signal). Result: absentee_score=7.9/10, did not saturate, but the formula has a real ceiling effect for corridors combining even moderately-high percentages (raw/10 exceeds the 1.0 cap easily), which would tie multiple "bad" corridors at 10.0 with no further differentiation.
[2026-07-05] User asked to add NoBroker and 99acres as additional scrape sources, explicitly prioritizing NoBroker because it shows owner name and phone number without login - stated goal was using phone number as "the most valuable field for NRI lead identification." Declined this specific objective again (same as the 2026-07-04 phone-number decision) - it directly conflicts with the project's own DPDP "aggregate only, no named individuals" boundary from the kickoff brief, and cross-referencing name+phone with inferred NRI status is individual-level sensitive-attribute profiling regardless of which site exposes it. Proceeded with recon on the legitimate parts (source accessibility for aggregate listing/volume/staleness data) as a boundary separate from the PII objective.
[2026-07-05] NoBroker listings API recon: the user-cited endpoint (api/v1/property/list, attributed to an "Apify actor") responded 200 but with "no property found" for reasonable parameter guesses - this does not appear to be the endpoint the live site actually uses. Network capture of the real site (typing a locality into the search box, clicking the resulting suggestion) confirms the actual listings endpoint is api/v3/multi/property/BUY/filter (same one found 2026-07-05 during Phase A recon), but even driving the full UI flow, the resulting filter call still showed unfiltered params (isCanonical=undefined, undefined=) - locality-scoping did not cleanly propagate through in this test. Confirms the earlier assessment: not a clean API integration, would need substantially more UI reverse-engineering with uncertain payoff.
[2026-07-05] 99acres re-recon: tried mobile subdomain (m.99acres.com, DNS does not resolve), mobile path (www.99acres.com/m/..., 403), sitemap.xml (403), robots.txt (403), RSS guess (403) - all blocked with the same Akamai edgesuite.net "Access Denied" response as the original 2026-07-03 finding. Notably even robots.txt is blocked, which is unusual (most sites intentionally keep it public) and suggests an IP/network-level block at the CDN edge rather than a page-specific rule - meaning no URL pattern variation is likely to bypass it from this environment.
[2026-07-05] Built `pipeline.py` (new module) + `run --city X --dry-run` CLI command to prove city-agnosticism before any Phase B scraping: loads city config, runs corridor discovery, generates sample MagicBricks URLs, touches no network. Verified across all 5 registered cities (Pune 10 corridors, Mumbai 10, Bangalore 8, Hyderabad 5, Chennai 4) - each showed its own corridor list and correctly city-parameterized URL with zero code changes between runs, only `--city` swapped.
[2026-07-05] Fixed the 3 flags raised in Phase B pre-flight: (A) replaced the hard `min(raw/10, 1.0)` cap with `10*(1-exp(-raw/5))` in a new `scoring/corridor_absentee_config.py` - verified raw=10.5 and raw=12 now produce different scores (8.8 vs 9.1) instead of both pinning at 10.0. Note: the curve still rounds to 10.0 for raw >= ~27 at 1-decimal display precision - an inherent property of any bounded+rounded scale, not fixable without either more decimal places or an unbounded scale, but the practical ceiling moved from raw=10 to raw~27, covering the realistic range much better. (B) Checked whether a stronger vacancy proxy (possession_date in the past but still listed) is buildable: no - MagicBricks exposes a status category, not a possession date field, and of 870 currently-scraped Pune listings, 0 have a structured possession date and only 2 mention one in free text. Skipped per instruction not to fabricate. (C) Replaced `below_market_pct` with `distressed_pct` (scoring/corridor_absentee.py): price-per-sqft (not raw price, consistent with the existing Tier-3 signal), tightened to 15%+ below corridor median (from 10%), and required co-occurrence with 60+ days listed. On real Hinjewadi data (n=600) this dropped the metric from 34% to 0.33% - lower than the user's own predicted 5-10% range, confirming the original below_market_pct was almost entirely normal BHK-size-driven price/sqft variance, not distress signal.
[2026-07-05] New corridor-level Absentee Score (scoring/corridor_absentee.py) is a separate module from the existing per-listing/per-society scoring/absentee.py (Tier 1/2/3 lead scoring) - the corridor-wide pass reads raw_listings directly with zero Gemini dependency, by design (Phase B decision: no quota risk on the wide pass, Gemini reserved for the Phase D society drill-down on the top 5 corridors only).
[2026-07-05] Diagnosed whether the zero-strong-NRI-signal result is real or an artifact, using only already-scraped data (870 Pune listings), no new scraping: (1) phone numbers - confirmed structurally absent from every scraped field (regex for Indian mobile + international prefixes, 0/870 hits) - MagicBricks gates phone behind a click-through, never renders it as text. Real limitation. (2) Truncation - checked description length distribution (12-2744 chars) and actual text at the cutoff for shortest/median/longest: every one ends in a complete sentence, none cut off mid-word. The "Read more" UI link is a CSS visual clamp, not a content gate - our scraper already captures full description text, not a preview. Ruled out as an artifact source. (3) Detail pages - we have never visited an individual listing's own detail page (only search-result cards), so whether it holds additional owner-status content is a genuinely open, unverified question (checking it would require new scraping, out of scope for this diagnosis). NoBroker - no new test run, relied on the immediately-prior recon (locality filter never worked even via full UI-driven session, so no confirmed field list exists to report). Verdict: mixed, not clean artifact-or-real - phone gap is real but a compliance non-starter regardless; text-based signals being empirically rare appears to be a genuine property of how listings are written at the search-card level, not a scraping hole; detail pages are the one unresolved variable.
[2026-07-05] Reweighted the corridor Absentee Score to 3 signals per user decision: dropped avg_age_days entirely (redundant with stale_90d_pct - same underlying days_since_posted data; and structurally incapable of mattering, max contribution 0.2 vs stale's 35). New weights: stale_90d_pct=0.45, vacancy_pct=0.35, distressed_pct=0.20. avg_age_days still computed and stored for information, no longer feeds the score. Verified on real Hinjewadi data (n=600): raw=3.566, absentee_score=5.1 (up from 4.3). Component breakdown: stale_90d_pct=88.3% of raw, vacancy_pct=9.8%, distressed_pct=1.9%. This EXCEEDS the user's own ~70% single-signal concern threshold (and their 85%+ "tell me honestly" threshold) - reported plainly rather than softened. Confirms this is fundamentally a staleness model with two rare bonus signals, not a balanced 3-signal model, given current real Pune data.
[2026-07-06] Built storage/listing_dedup.py: matches on (society, price_inr, area_sqft) signature, confirmed via description-similarity (corridor names stripped first) since a large project can have many genuinely-different units at the same price/area. Marks duplicates via a new `duplicate_of_id` column (nullable self-referential FK on raw_listings) rather than deleting - which corridors a property appeared in stays queryable. Deliberately does NOT change per-corridor scoring queries: a broker searching Hadapsar would still see a property also tagged Magarpatta Road, so corridor-level stats keep counting every corridor a listing was visible in. Dedup exists specifically to fix analyses that break under double-counting the same property as two distinct ones (Signal 1, multi-property owner).
[2026-07-06] Lesson: my first-pass dedup threshold (0.85 description similarity) was miscalibrated - too strict, verified by manually reading two confirmed-genuine duplicate pairs (same address/RERA number/specs) that scored 0.808 and 0.550, both below the cutoff. MagicBricks' template system reworks otherwise-identical facts into different sentence structures, so pure text similarity varies a lot even for true duplicates. Lowered to 0.6 as a reasoned middle ground (catches the 0.808 case, not the 0.550 outlier) - explicitly flagged as a judgment call, not a validated threshold, given only 2 data points. Also fixed a real bug: difflib.SequenceMatcher treats two empty descriptions as a perfect match (ratio 1.0) - added an explicit guard so listings with missing description text are never merged (absence of evidence, not evidence of a match).
[2026-07-06] Final dedup reconciliation on the 870-row Pune dataset: 851 canonical (19 marked as duplicates - 15 cross-corridor groups, mostly Hadapsar/Magarpatta Road, plus 2 same-corridor repeat-listing groups). This differs from an earlier ad-hoc check (which had estimated 831 canonical, 39 duplicates) - the ad-hoc version used exact string matching with no description-similarity confirmation, and was catching coincidental price+area+society matches (e.g. a large project like Shapoorji Pallonji Joyville Hinjewadi selling many identical-configuration units at the same price) as false "duplicates." The rigorous, confirmed number is 851, not 831.
[2026-07-06] Rental recon for Hinjewadi (60 listings, 2 pages, `flats-for-rent-in-hinjewadi-pune-pppfr` - a genuinely different URL structure from the sale pattern, not just a suffix swap, confirmed by testing 3 candidates). Rent cards expose fields sale cards don't: `furnishing`, `tenent-preffered`, `super-area`, plus a structured JSON-LD RentAction block with a `landlord.name` field. Checked all 4 requested signals: (1) furnished+owner-listed: 26.7% raw, but checked whether furnishing correlates with owner-vs-agent specifically - it doesn't much (22.2% agent-listed vs 31.4% owner-listed fully furnished, small sample, not a clean split) - furnishing looks like a generic feature of this rental market, not a distinctive absentee marker; (2) investor/rental-income framing from the owner side: 0% genuine (the only keyword that fired, "no brokerage," is redundant boilerplate for owner-posted listings, not a distinct investor-framing signal); (3) owner renting multiple units: 0/51 in this 60-listing sample (likely underpowered - even sales' 831-listing sample only found ~1% multi-property hit rate, a 60-listing rental sample can't reliably detect a signal that rare); (4) long-term-tenanted/owner-managed-remotely language (currently tenanted, existing tenant, managed by, caretaker, NRI owner, owner abroad, POA): zero hits, all of them - same pattern as sales, now confirmed in rentals too.
[2026-07-06] Listing-signal exploration formally closed per user decision: ~1% ceiling confirmed across sales and rentals, registry pivot (EC/MahaRERA) is the path for real NRI/absentee identity, not further listing recon.
[2026-07-06] Shipped output/absentee_candidates_pune.csv: 10 rows from validated signals only (Signal 1 high-confidence multi-property: 4 distinctive names x 2 listings each = 8 rows; Signal 3 genuine never-occupied: 2 rows), across all 851 canonical Pune listings, all 10 corridors. Declined to include a raw `owner_name` column even though requested - same DPDP reasoning as the phone-number decisions (2026-07-04, 2026-07-05): name + address + an absentee-signal flag is exactly the "compiled list of named individuals" the kickoff brief said to avoid, even without a phone number attached, and it isn't operationally needed for the stated next step (registry-checking a property is done by society/address, not by a listing's self-reported seller name). Used `listing_id` (internal DB reference) instead. `listing_url` is genuinely empty for all rows - the scraper never captured detail-page hrefs for these listings, not fabricated. `property_address` is empty for 9/10 rows - only one listing (id=628) had an extractable address-like text fragment; the rest never mention a structured address in free text. `score` is literal per signal (property count for multi-property, a confirmed-genuine marker for never-occupied), not a composite formula - user explicitly said no formula rewrite this round. `days_since_posted` included as a separate, unweighted supporting column for staleness as instructed (tie-breaker, not baked into score).
[2026-07-06] Task 1 (traceability recovery): re-located 7/10 flagged listings by matching society+price+area against live corridor search pages (up to 6 pages Hinjewadi, 2 pages others) - 301, 402, 285 not found within that range, reported as unavailable (likely delisted/sold since original scrape) rather than expanding search indefinitely. Of the 7 recovered, only 2 (628 Sai Arcade, 783 Venkatesh Sharada Megh Sparsh) yielded a genuine street-level address/PIN; the other 5 only confirm project name + corridor (already known). Caught and corrected a real bug in my own recon: the regex used to find address fragments was matching RERA registration numbers (long digit strings) as if they were 6-digit PIN codes - re-checked all "address candidates" manually and excluded the false positives before reporting. For 628, 783, 726/727, confirmed the flagged owner name is actually present in the live page text (Rishabh Singh, amol amol, Tilak Raj) - direct confirmation the Signal 1 matching was correct, not a coincidence. For 139/140 (Xrbia Hinjewadi), could NOT confirm the flagged name ("shivam pandey") in the visible page text of the project aggregator page - flagged as an open uncertainty, not silently resolved either way.
[2026-07-06] Task 2 (bounded registry check): checked IGR Maharashtra's actual E-DISPlay search form (pay2igr.igrmaharashtra.gov.in) before attempting any lookup rather than assuming address would work - confirmed it requires District/Taluka/Village PLUS a Document/Property/CTS/Survey number, not an address or project name. We have none of the required survey/CTS/document identifiers for any of the 10 listings (MagicBricks never exposes this), so a real EC/sale-deed lookup is structurally blocked for all 10, independent of how good the address recovery was. MahaRERA (maharera.mahaonline.gov.in) remains unreachable from this environment (same redirect-to-unreachable-domain finding as 2026-07-05) - and independent of reachability, MahaRERA tracks project-level registration compliance by developers, not individual unit-owner records, so it structurally could not answer POA-execution or owner-foreign-address questions even if accessible.
[2026-07-06] Extra municipal-portal check per user request: neither PMC (propertytax.punecorporation.org) nor PCMC (publicptaxpcmc.in) accept address or society-name search. PMC's homepage search box is generic website-content search, not a property lookup (confirmed by testing - it returned generic site content, not property records). PCMC's own field label states outright "Search by Property / Mobile no." - tested with a real address anyway, got an empty response. Same structural gap as IGR: every public portal checked needs a government-issued identifier as the search key, none accept address. Listings-to-registry bridge confirmed closed via public self-service portals.
[2026-07-06] Moving to production execution (Asset 1 scale-up + Asset 2 directory) per user decision. Added `detail_url` capture directly to scraper/magicbricks.py's RawListing (the card's href) - eliminates the need to re-locate listings after the fact for detail-page recovery, which was the main friction point in the 10-listing test.
[2026-07-06] Pre-flight URL verification before mass-scraping the 4 new cities (23 corridor URLs, 1 page each) caught 3 more real slug mismatches, same pattern as Pune's Magarpatta: Hyderabad's "HITEC City" 404s, real slug is "hitech-city" (one word) - renamed the corridor to "Hitech City" throughout. Mumbai's "Thane" and "Navi Mumbai" both 404 under the "mumbai" city slug - turns out these are independent municipalities with their OWN separate city slugs in MagicBricks' taxonomy (e.g. "thane", "navi-mumbai"), not localities within Mumbai proper; our CityConfig model assumes one city_slug per config, so properly supporting them would mean a per-corridor city-slug override, out of scope for this round. Substituted two verified Mumbai-proper localities instead: Mulund (replacing Thane) and Borivali (replacing Navi Mumbai). All other 20 corridor URLs across Mumbai/Bangalore/Hyderabad/Chennai verified working on the first try.
[2026-07-06] Built scoring/validated_signals.py: the production version of the two signals validated during diagnosis, reusable identically across all 5 cities. Signal 3's keyword set was refined from the original 5 keywords to 4, dropping bare "brand new" - manual verification during diagnosis found it was responsible for every false positive (marketing boilerplate) while every confirmed-genuine hit also had "never used"/"unused" co-occurring. This lets the rule run unsupervised at full scale without a human re-reading every hit, which isn't feasible at thousands of listings the way it was at 8.
[2026-07-06] Track 2 (NRI community directory): verified "whtspgrouplink.com" is a real site (confirmed via web search, has an actual "NRI Real Estate WhatsApp Group Links" page) - it just happened to be consistently unreachable from this environment (connection timeout, tried twice). Used two working alternatives instead (grouplink.com.in, whtsagrouplinks.com) from the same search results. Found a real, evidenced data-quality problem, not just generic caution: scraped 174 raw link+name pairs across both sites, but only 73 are actually distinct links - and of those 73, **28 (38%) have conflicting group names attached to the identical link across the two source sites** (e.g. the same chat.whatsapp.com link is labeled "Earn daily uk" on one site and "SMART X" on the other). This means the "group_name" field in this kind of aggregator is not reliably tied to what the link actually leads to. Also spotted specific irrelevant/suspicious entries by name alone during review (e.g. "Earn daily uk", "THE BILLIONAIRES MIND SET", emoji-heavy "get rich" style names, non-India entries like Zanzibar/Paris) mixed into the same "NRI Real Estate" category with zero apparent moderation. Deduplicated to 73 rows by link URL (not name, since name isn't trustworthy), populated city_focus only where a city name is explicitly present in the group name text (21/73), left blank rather than guessed otherwise.
[2026-07-06] Found and fixed 3 real dedup bugs during the first full-scale (2100-listing) production run for Pune, each caught by manually verifying WHY specific Signal 1 (multi-property owner) hits looked suspicious rather than trusting the count: (1) the empty-description guard (correctly conservative in principle) left genuine duplicate pairs unmerged when MagicBricks failed to render a description for one side - fixed with an owner-name-exact-match fallback confirmation path. (2) The core algorithm only ever compared each listing in a signature group against the group's first (lowest-id) member, never against each other - so when a group contained two DIFFERENT owners' listings that were each genuine cross-corridor duplicates of each other (not of the group's first item), the true match was never checked. Fixed by greedily clustering within each signature group instead of star-comparing against one fixed representative. (3) `_signature()` returned None (excluding the listing from dedup entirely) whenever carpet area was unparseable, hiding at least one confirmed duplicate pair (same owner, 0.62 description similarity) from ever being compared. Fixed by falling back to (society, price, None) when area is missing, still gated by the same confirmation logic so this only widens who gets compared, not who gets merged. Net effect on Pune's first full run: validated candidates dropped from an inflated 120 to a verified 30 as each fix landed (120 -> 40 -> 32 -> 30).
[2026-07-06] Pune (30 candidates, detail pages recovered), Mumbai (11) and Bangalore (10) production runs completed clean with the fixed dedup logic - no further bugs surfaced, overlap rates (Mumbai 9/2100, Bangalore 0/1680) plausible given those corridors are geographically distinct localities.
[2026-07-06] Hyderabad's first full run flagged 103/1050 candidates (~10%), a 10x outlier against every other city (Pune 3.5%, Mumbai 0.5%, Bangalore 0.6%) - investigated rather than shipped, per the same rule that caught the Pune dedup bugs. Root cause: Hyderabad's MagicBricks card layout has no `data-summary='society'` element at all (confirmed live - its cards only carry carpet-area/status/floor/transaction/furnishing/facing/overlooking/ownership/parking/bathroom/balcony, never society), so `society_name_raw` was `None` for all 1050 listings (vs 6.5% blank in Pune, a normal baseline). This silently broke dedup entirely for the city (`_signature()` requires a society name, so nothing was ever compared - explains the suspicious `marked_duplicate=0`), so genuine cross-corridor duplicates went unmerged and inflated Signal 1 by double-counting the same physical listing under two corridor tags as two owned properties (confirmed concretely: listing 7237 tagged "Gachibowli" and 7421 tagged "Kondapur" share the identical detail_url, both attributed to owner "Vijay Reddy"). Fix: added `_society_from_url` to scraper/magicbricks.py, parsing the project name out of the detail-page URL slug (e.g. `.../my-home-bhooja-hitech-city-hyderabad-pdpid-...` -> "My Home Bhooja") as a fallback whenever the card-summary field is empty. First version only stripped the search corridor's own slug and would have re-broken the exact cross-corridor case it was meant to fix (the Gachibowli-found copy and Kondapur-found copy would derive different fallback names, e.g. "Indis Viva City Kondapur" vs "Indis Viva City", and dedup would still never compare them) - caught before shipping by testing the specific known-duplicate pair, fixed by stripping against every corridor slug in the city (not just the search one), same principle as `_strip_locality_tokens` already used for description similarity. Backfilled the already-scraped 1050 rows in place from their stored `detail_url` rather than re-scraping (826/1050 had a usable URL; the other 224 lack one, a separate pre-existing gap - a card's href occasionally fails to capture, same class of gap already documented for detail-page recovery). Re-ran dedup and signals after backfilling: overlap went from a suspicious 0% to a plausible 10.7% (938/1050 canonical), and validated candidates dropped from the inflated 103 to 52. Spot-checked the 52: the named-society pairs (e.g. "Aarthi Residency", "Dsr Reganti", each appearing exactly twice) survived dedup and are plausible genuine same-owner-two-properties hits, not undetected duplicates. 36/52 still show a blank society because their underlying rows are in the 224 that never had a `detail_url` to fall back on - accepted as the same pre-existing, already-documented limitation rather than a new gap. Applied the `all_corridors` fix prospectively in `scrape_corridor`/`run_production_pipeline.py` so Chennai (not yet scraped at time of this fix) gets correct society names from the first pass, not another post-hoc backfill.
[2026-07-06] User audit of the shipped 114-row combined CSV found concrete evidence Signal 1 looked wrong again: 16/50 URL-bearing rows were 2 listing_ids on the identical MagicBricks project-page URL, and owner_note (from recover_detail_pages) often showed 3-8 unrelated names rather than confirming the flagged owner. Asked for a direct yes/no on whether find_multi_property_owners had regressed to URL-based matching. Verified against the actual code: no - `_canonical_rows` still restricts to `duplicate_of_id IS NULL` rows and grouping is still by normalized `posted_by_raw` name, unchanged from the validated design. But chasing the concrete evidence surfaced two real, separate bugs the "no regression" answer doesn't excuse: (1) **Bangalore was 100% blank society_name_raw** - the identical Hyderabad bug, just never re-checked on the other cities that were scraped before that fix existed (Mumbai was also elevated at 13.4% vs Pune's 6.5% baseline). Ran the same URL-slug backfill across all 5 cities uniformly this time instead of assuming Hyderabad was the only city affected: Bangalore 1417/1680 rows recovered, Mumbai 12/281. (2) **"Owner: Magicbricks User" / "Owner: MB User" are the site's own placeholder text**, not real names, confirmed via 5 occurrences across 4 different cities with completely unrelated societies/prices/corridors - but as two-word strings they passed the existing "2+ word name" distinctiveness filter and grouped as if the same person owned multiple properties. Added an exact-match exclusion set (`PLACEHOLDER_OWNER_NAMES`) to scoring/validated_signals.py. A third bug came directly from tracing one specific piece of the user's evidence (Bangalore id 6122 vs 6327, "Rudrani Aum Apartment", same price/owner/near-identical description/same detail_url, yet neither marked as the other's duplicate): the "area None fallback" added earlier only bridges two listings that BOTH lack a parseable area - it never bridges one with a real area against one without, since `(society, price, 1234)` and `(society, price, None)` are different dict keys and are never placed in the same comparison group. Fixed by dropping area from `_signature()`'s key entirely - it's now `(society, price)` only; `_is_confirmed_duplicate`'s actual merge gate (description similarity or exact owner match) is unchanged, so this only widens who gets compared, same philosophy as the original None-fallback fix, just applied completely this time. Re-ran dedup + signals across all 5 cities after all three fixes: Pune 30 (unchanged), Mumbai 11 (unchanged), Bangalore 10 -> 8, Hyderabad 52 -> 50, Chennai 11 -> 9. **Corrected combined total: 108 rows** (was 114). Re-ran the same shared-URL audit that started this: dropped from 10 shared-URL pairs to 7. Traced each remaining pair individually rather than assuming they're clean: one (Kalpataru Jade Residences, listing 1655 vs 1639) turned out to be two independently-valid, unrelated flags (Signal 1 and Signal 3 on different listings) that only coincidentally share a project-level URL, not a grouping error. Four are genuine same owner-name-key groups where MagicBricks' detail_url is a project overview page rather than a unit page, which makes owner_note's "who's on this page" check weak evidence either way (a big project's page lists many unrelated sellers) - two of these four are well-corroborated by distinctive names appearing directly in the page text (Giriraj Mantri/Dsr Reganti, VENKAT REDDY SRIPATHI/Aarthi Residency), and two carry real, disclosed collision risk because the names are common (Vinay Kumar/Bscpl Bollineni Bion, Sanjay Varma/Dlf Gardencity - the latter also spans a 7.5x price gap, 7.50cr vs 1cr, in one of Chennai's largest townships) - this is the pre-existing, already-documented "heuristic, not verified identity" limitation surfacing concretely, not a new bug, and not something further code changes can resolve. Separately confirmed Hyderabad's 36 blank-society/no-URL/owner_note=N/A rows are an explicit early-return guard in `recover_detail_pages` (`if not url: ... continue`), not a silent failure - verified directly from the code rather than re-reading the CSV mid-rewrite.
[2026-07-06] Built the society-level PRODUCT-FIT layer (rung 3: city -> corridor -> society -> people), scoring/society_fit.py + scoring/society_fit_config.py + scoring/maps_enrichment.py - a third, independent score, never merged with corridor fit_score or absentee_score. Key design decisions: (1) Added `product_price_band_min_inr`/`max_inr` to CityConfig rather than hardcoding a band in the scorer - populated Pune's with the exact values already locked in scoring/society_config.py (Rs 75L-1.55Cr) as literal duplicated constants, not an import, since config/ is upstream of scoring/ in this codebase's dependency direction. Mumbai/Bangalore/Hyderabad/Chennai deliberately left unset (None) - no business-confirmed band exists for them anywhere in the codebase, and the hard constraint explicitly bars falling back to Pune's numbers, so the honest behavior is to skip them with a clear reason, not guess. (2) Added `normalize_society_name()` (storage/entity_resolution.py) as a new sibling to the existing `normalize_name()`, not a modification of it - owner-name matching (validated_signals.py, listing_dedup.py) still uses the original; only the society-fit layer's grouping uses the new one, which additionally strips trailing noise words (apartment/society/chs/phase N/wing X) via repeated regex passes so compound tails like "... Phase 1 CHS" reduce fully. Verified against real Pune canonical_societies names before wiring it in (e.g. "Vilas Prime Panache C Wing" -> "vilas prime panache", "Paramount Madhupushpa Phase 1 CHS" -> "paramount madhupushpa"). Does NOT fuzzy-merge spelling variants ("Well Wisher Kiara Terrezo" vs "Wellwisher Kiarah Terrazo" stay separate groups) - out of scope, only the specific noise-suffix rule was asked for. (3) Repurposed cli.py's existing `societies` subcommand rather than adding a new verb: it previously called scoring/join.py's build_ranked_society_table against `classified_listings`, which has stayed at 0 rows since the pipeline moved to the deterministic raw_listings path (never populated in production) - a dead command, not a validated signal, so pointing the same verb at the new layer isn't "redefining an existing signal," it's retiring scaffolding that was never actually running. Made `--corridor` optional: given, filters to one corridor; omitted, shows every corridor that passes the existing 5-gate corridor engine (scoring/corridor.py rank_corridors, eligible=True) in ranked order - ties rung 2 to rung 3 naturally by reusing that engine's output as the "top corridors" selection rather than inventing a new one. (4) Dedup respected exactly as instructed (canonical rows only, same `duplicate_of_id IS NULL` + city-wide query pattern as validated_signals.py's `_canonical_rows`) - this means a physical property that straddles two corridor searches contributes to exactly one corridor's metrics (whichever its canonical row is tagged with), consistent with "never double-count," even though it's a subtly different scope than scoring/vacancy.py (an older, pre-dedup module that intentionally counts every corridor a listing was visible in, per its own docstring - the two modules' scoping differs because vacancy.py predates listing_dedup.py entirely, not because of an inconsistency introduced now). (5) Percentile-rank normalization implemented as a plain O(n^2) manual rank (no scipy dependency) - fine at the actual scale (tens of qualifying societies per city). Independently verified by hand-recomputing one full example (Godrej Elements, Hinjewadi) from raw DB rows outside the module's own code and confirming the exact same 8.5 score, not just that the code ran without error. (6) Google Maps enrichment (scoring/maps_enrichment.py) is physically separate from the deterministic metrics and calls the Places API Text Search endpoint (a REST call, not Gemini/an LLM) - found and fixed a real caching bug before shipping: an invalid/rejected API key (tested with a deliberately fake key against the real endpoint) initially got cached as a permanent "no match" identically to a genuine zero-result search, which would have silently kept returning stale failures forever even after a valid key was added later. Fixed by distinguishing Places API statuses - `ZERO_RESULTS` (and `OK` with an empty results list) is a genuine, cacheable non-match; `REQUEST_DENIED`/`OVER_QUERY_LIMIT`/`INVALID_REQUEST`/`UNKNOWN_ERROR`/network exceptions now raise `MapsApiUnavailable` and are explicitly never cached. Confirmed the fix with the same fake-key test: 0 rows written to maps_cache afterward. Dry-run across all 5 cities (`build_society_ranking.py`), same code path, zero city-specific branches: Pune scored (50 societies qualified across its 10 corridors, MIN_LISTINGS=3, in-band Rs 75L-1.55Cr only), Mumbai/Bangalore/Hyderabad/Chennai each cleanly skipped with "no product_price_band configured" (this environment has no GOOGLE_MAPS_API_KEY set, so all 50 Pune rows correctly show rating_available=0 - the graceful-degradation path is genuinely exercised, not just theoretically present). Track-1 absentee-candidate linking confirmed working on real data (3 of the 50 qualifying societies have 1 linked candidate listing each), and the cross-corridor contamination flag (appears_in_corridors) is wired but shows 0 contaminated societies in this run - Pune's existing dedup plus the in-band/MIN_LISTINGS filtering narrows the qualifying set enough that no normalized society happened to clear the bar in two different corridors this time, not a sign the flag doesn't work.
[2026-07-06] Built the BROKER-LIST layer (rung 3b: agents/brokers active in a corridor), scoring/broker_list.py - a fourth, independent score (broker_activity_score), never merged with fit_score/absentee_score/society_fit_score. Extracted the percentile-rank helper out of scoring/society_fit.py into a new scoring/stats_utils.py first, so both layers share one definition instead of two copies (society_fit.py re-verified against a fresh run afterward, unchanged 50/50 result). No scraper change was needed - posted_by_raw (the poster-identity field) was already captured; confirmed this live rather than assuming, with three separate checks before writing any extraction code: (1) an agent card's ads block HTML has no separate phone/company element, only the name text and a generic "buyers served" badge; (2) an agent-posted listing's detail-page body text shows "Agent: <name> / Contact Agent" with no rendered number anywhere; (3) the full card HTML confirms phone sits behind a "Get Phone No." click-through button, the same gate already documented for owner phone numbers (DECISIONS.md 2026-07-05) - never a rendered value. Conclusion: phone and company are set to NULL for every broker, permanently, by design - not a gap a scraper change could close (the site structurally never renders either without further interaction), and posted_by_raw does not separate a person's name from an agency's name into two fields in the first place (the same single text is sometimes obviously a person - "Shubham" - and sometimes obviously a company - "Dream Home Real Estate Consultancy" - with no marker distinguishing which), so `company` is never guessed from the text either. Reused parse_listing_type's existing owner/agent split exactly as instructed (brokers = rows classified 'agent', which already covers dealer/agent/builder prefixes) and PLACEHOLDER_OWNER_NAMES from validated_signals.py for "Magicbricks User"/"MB User". Found two MORE platform-placeholder variants during this layer's own dry-run, verified before excluding: bare "Agent: Magicbricks" (3 rows, Pune+Chennai, completely unrelated societies/prices) and bare "Agent: Real Estate" (5 rows, Pune+Mumbai+Bangalore, completely unrelated societies/prices) are generic labels, not real one-off agency names. Checked whether either could be inflating the ALREADY-SHIPPED Track 1 owner-side numbers before dismissing this as broker-only: "magicbricks" is a single word, already excluded there by the existing 2+-word distinctiveness filter regardless; "Owner: Real Estate" was checked directly and returns 0 rows, so it never reached that signal either. Added both to a broker-layer-local set (`_BROKER_PLACEHOLDER_NAMES`) rather than editing the shared, already-audited constant, since brokers have no word-count filter (legitimate one-word agency names like "Rushub" are common) and the fix doesn't belong to the owner signal. Implemented the double-count guard exactly as specified: counts distinct detail_url values per (corridor, agent) group, falling back to a plain row count for the rows that never had a capturable URL (a separate, pre-existing gap) rather than silently miscounting them - confirmed this guard has real, non-theoretical effect by finding a genuine case in Pune's data ("Prathmesh Properties", Viman Nagar, the identical detail_url appearing twice among canonical rows) and verifying the shipped output correctly shows listing_count=1, not 2, for it. Found and fixed a real data-integrity bug while re-running after adding the two new placeholder exclusions: broker_meta had 1577 rows in the DB but the freshly-regenerated CSV only had 1571 - the 6 missing were stale "magicbricks"/"real estate" rows from the PRE-fix run that an upsert-only write (INSERT OR REPLACE) can never remove, since it only ever touches rows sharing a NEW run's primary keys, never deletes rows the new run simply doesn't produce anymore. Fixed by having compute_broker_list unconditionally DELETE that city's existing broker_meta slice before recomputing, on every call path (not just the success path) - confirmed the fix by re-running and checking DB row count (1571) exactly matches the CSV, zero placeholder leftovers. Applied the identical fix to scoring/society_fit.py's compute_society_fit too, even though it hadn't been empirically triggered there yet - it has the exact same upsert-only write pattern and is exposed to the same latent risk, so leaving it unfixed once the bug class was known would have been inconsistent. Dry-run across all 5 cities (all already have scraped data from Track 1, so this needed no new scraping): Pune 279 brokers, Mumbai 546, Bangalore 461, Hyderabad 165, Chennai 120 - same code, zero city-specific branches, real difference is only in each city's underlying data volume. Declined the optional JustDial/Google-Maps agent-lookup source: it would mean scraping a brand-new site with no prior recon in this project (JustDial has no public API and, per this session's established pattern with 99acres/NoBroker, would need real verification of reachability/reliability before trusting it), which isn't "fast and non-blocking" as the brief's own optionality condition requires - explicitly skipped rather than rushed.
[2026-07-06] Built web/ frontend (React + TS + Vite + Tailwind), a static SPA reading JSON exported by export_frontend_data.py - the export script is the sole Python-to-frontend bridge, calling existing rank_corridors/_canonical_city_rows and reading existing CSVs/tables rather than recomputing any of the 4 independent scores. Verified end-to-end 2026-07-06: clean production build, dev server serving all 7 data files with numbers matching the shipped CSVs.
[2026-07-06] UI round 3 (images + why-it-works numbers): built a manifest-based image system for web/ - web/public/assets/image-manifest.json (namespaced keys "City", "City::Corridor", "City::Corridor::Society" to avoid cross-city collisions) + web/src/lib/images.ts (fetch-once hook, mirrors DataProvider's pattern but deliberately NOT merged into it, since export_frontend_data.py is documented as the sole Python-to-SPA bridge and this is manually curated art direction, a different data source) + web/src/components/ImageTile.tsx (exact-match lookup, falls back to the existing PlaceholderTile unchanged when no entry exists - a manifest entry only ever adds a photo, never removes the honest default). Wired into CityExplorer (city tiles), CityView (corridor tiles), and added new to SocietyFinder (40px society thumbnail, not previously image-bearing). No live scraping or hotlinking path added anywhere - confirmed by design, matches the explicit brief ruling out both (generic locality photos aren't freely licensed/reliably hotlinkable; named-society developer photos are copyrighted marketing material) - actual images are a one-time manual drop into web/public/assets/images/ by the user, none supplied yet, so every tile still renders the placeholder today. Corrected the Sidebar's footer claim ("No manual entry") to disclose imagery as the one manually curated exception, since it would otherwise misrepresent the new manifest as engine output.
[2026-07-06] Rejected the brief's specific "McKinsey 5 to 10% higher ROI" citation for the Home page's why-it-works section after checking it against McKinsey's own site (web search): no McKinsey primary source states that exact figure as an ROI claim - it traces to secondhand vendor-blog paraphrasing, same problem class as the vendor stats the brief was already rejecting. Found and used a genuinely verifiable substitute instead: McKinsey's "Six new imperatives for real estate players" states digital/analytics adoption can increase net operating income (NOI) by 10 percent or more - a different, correctly-scoped metric (NOI, not ROI), attributed and linked directly to the source article. The four StatRow numbers themselves (brokers indexed, absentee candidates, cities scored, directory links) were already 100% CSV-derived with no vendor numbers anywhere in the codebase - confirmed by grep before touching anything, so that part of the brief's acceptance criteria was already met going in.
[2026-07-06] UI round 4 (confirm-and-tighten): exercised the image-manifest hit-path for the first time - added a real entry, "Pune::Hinjewadi" -> a licensed Unsplash photo (Declan Sun, "modern office building with glass facade," Unsplash License confirmed live via unsplash.com/license before downloading, no attribution legally required for a standard download/non-API use but credited anyway in the manifest's new `_meta` block), downloaded to web/public/assets/images/, and marked `illustrative: true` with an explicit note that it's generic stock, not an actual Hinjewadi photo. Hardened web/src/lib/images.ts with a new exported `parseManifest()` that normalizes any non-object/wrong-shaped/wrong-typed manifest to empty buckets per-field rather than assuming the fetched JSON matches the expected shape (the original version cast `res.json()` straight to `ImageManifest`, which would have thrown inside ImageTile on a shape mismatch, e.g. a bucket key missing or holding the wrong type - a real gap, not yet exercised). Verified against the actual shipped code, not a reimplementation: ran `parseManifest` directly under `node --experimental-strip-types` against 8 cases (valid, {}, null, array, string, wrong-typed bucket, non-string bucket values, extra _meta/_comment keys) - all normalized cleanly, zero throws. Separately verified the full fetch chain against the live dev server for a real 404 and a deliberately malformed-JSON scratch file (both degraded to empty buckets, confirmed via the real running app, not a mock) - scratch file deleted after the check. Confirmed via curl that the new image itself is served correctly (200, image/jpeg, correct byte size) and that the JS bundle size is unchanged (image is a public/ static asset referenced by URL, never bundled).
[2026-07-06] Corrected the Home page's McKinsey citation a second time after user pushback: the round-3 substitute (NOI +10% from digital adoption) is a real McKinsey figure but the wrong topic for a "why data-driven EXPANSION works" section - it's about operating existing properties more efficiently, not choosing where to expand, a mismatch a careful reader would catch. Replaced with the same report's actual site-selection finding instead: a McKinsey-built rent-growth model for a US West Coast city found proximity to gas stations hurt rent growth while proximity to many five-star-reviewed restaurants helped it - a real location-analytics result, same primary source (mckinsey.com/industries/real-estate/our-insights/six-new-imperatives-for-real-estate-players, March 2023), verified via two independent searches after WebFetch repeatedly failed to load the live page directly (ECONNRESET) - written as an attributed paraphrase, not a verbatim quote, since the exact sentence text couldn't be directly confirmed. Also tightened the Sidebar footer copy: the prior wording ("no manual entry... tile imagery is the one manually curated exception") technically read as an exception carved out of the data-provenance claim itself; reworded so imagery is described as a separate, non-data layer ("not analytical data") rather than an exception to the absolute machine-derived-data claim, so the two claims can't be conflated.
[2026-07-06] UI round 5: committed the parseManifest robustness check as web/src/lib/images.test.ts (vitest, added as a new devDependency - web/ had no test runner before this), importing the real shipped parseManifest, all 8 shape cases from round 4's manual check. Proved the test is a real regression guard, not just accidentally green: deliberately broke one guard (the null-check in asStringRecord), confirmed the suite failed loudly with the exact underlying TypeError the guard prevents, then restored and confirmed all 8 pass again. `npm test` (vitest run) is now the repo's web/ test command.
[2026-07-06] Populated the image manifest across all three namespace levels (previously corridor-only) with real, curl-verified files: "Pune" (city) -> a generic Unsplash aerial-urban-area stock photo (Anand Dhumal), "Pune::Hinjewadi::Godrej Elements" (society, the top-ranked Hinjewadi society at 8.5 fit) -> a generic Unsplash apartment-building stock photo (Daniel Reis). Both checked against unsplash.com/license at pull time (2026-07-06, same as the round-4 Hinjewadi photo), both marked illustrative:true with an explicit per-entry note. The Pune city photo's note flags an extra honesty nuance found during sourcing: the search context surfaced it under a "Pune" tag, but the photo's own title ("aerial view of urban area") doesn't confirm the location, so the note says explicitly that geographic accuracy is unverified, not just "generic." The Godrej Elements photo's note is stronger still: it is NOT a photo of the real building (no rights to Godrej Properties' marketing photography exist), stated plainly so nobody mistakes it for the actual project. All three images confirmed served correctly via curl (200, image/jpeg, byte-for-byte matching the downloaded file size) before being called done. Bundle size unchanged after rebuild (still 405.79 kB), confirming images stay outside the JS bundle as public/ static assets, same as round 4.
[2026-07-06] UI round 6, part 1 (city-agnostic audit): grepped web/src for every corridor/society name in the slot table plus "Pune" itself - zero hits in actual app logic, only in web/src/lib/images.test.ts's example fixture data (arbitrary sample strings for testing parseManifest's shape-handling, not a UX literal) and the manifest/DECISIONS.md itself (legitimately city-scoped assets/logs, not code). Read every remaining page not yet reviewed in prior rounds (CorridorDetail, BrokerFinder, AbsenteeFinder, NriDirectory) - confirmed all of them were already 100% data-driven from when they were first built, consistent with this project's repeated "same code, zero city-specific branches" convention. Net new work here was smaller than the brief anticipated because the audit found the spine was already clean; added a persistent `CitySwitcher` (web/src/components/CitySwitcher.tsx, wired into Sidebar) since no always-visible way to jump cities existed before - city selection previously required going through Home's dropdown or City Explorer's grid. Verified Mumbai (real scraped data: 10 corridors, 546 brokers, 11 candidates, zero manifest image entries, no price band) renders end to end through the same code paths as Pune - no second "stub" city was fabricated since Mumbai already has real, non-placeholder data to prove the point honestly.
[2026-07-06] UI round 6, part 2 (dashboard -> source-legible product): read skills/frontend-design/SKILL.md and invoked the dataviz skill before writing any chart code, per both this project's Section 5 mandate and the round's explicit instruction. Built 3 new visual components, all reading directly from already-loaded DataProvider JSON, nothing mocked: (1) CorridorSignalChart - "movement across corridors," a single-hue (violet-500) horizontal bar chart of absentee-candidate count per corridor within the current city, sorted by magnitude - chosen per the dataviz form heuristic ("compare magnitude low->high" -> bar + sequential one hue, not a categorical palette, since corridors are being RANKED on one measure, not compared as coexisting identities). (2) StalenessHistogram - deliberately named and captioned as a listing-age DISTRIBUTION at time of scrape, not a "trend over time": days_since_posted is captured once per listing, never from repeat observations of the same listing, so a smoothed/continuous trend line would imply precision (a tracked rate of change) this single-snapshot data doesn't have. Binned bar chart (0-30/31-60/61-90/90+/unknown days) instead, explicit caption states the distinction. (3) PipelineLineage - the listing -> signal_matched -> address_recovered/owner_note -> score chain as a first-class 4-node visual per candidate in AbsenteeFinder, replacing the previous plain-text card; a stage with nothing recovered says "not captured" in muted styling rather than being silently omitted, so the chain never overstates confirmation. Ran the dataviz skill's validate_palette.js against the actual chart hue (#8b5cf6) on the app's real dark surface (#1e1a38, not the validator's generic default) - all 4 checks pass; did NOT run it against the pre-existing 3-way corridor/society/broker "kind" signal colors (unchanged from earlier rounds) since those are never rendered as a coexisting categorical set in one chart (each ScoreRing "kind" only ever appears alone, on its own dedicated page, with the kind already named by the page title/label) - validating them as a categorical group would test a usage pattern the app doesn't actually have.
[2026-07-06] Built SourceInspector (web/src/components/SourceInspector.tsx), a reusable provenance modal wired into the app root (App.tsx, inside DataProvider). Every headline number in StatRow, every corridor fit_score (CityView), every society_fit_score (SocietyFinder), and every broker_activity_score (BrokerFinder) is now clickable and opens a panel naming the exact source file/CSV, a plain-English summary of how the number was derived, and a sample of the real underlying rows - each row links to its actual detail_url where one was captured, not a synthetic example. This is built entirely from data already loaded via DataProvider (no new fetch, no recomputation of any score) - the modal is a presentation layer over the same JSON every page already reads. StatRow tiles required widening their prop from `stats: Stats` to `data: DataShape` (exported DataShape from DataProvider.tsx, previously module-local) so the modal's sample rows could pull real broker/candidate/directory rows, not just the aggregate counts.
[2026-07-06] UI round 7 (cinematic redesign): client saw round 6 and asked for a different visual direction with a supplied reference image. Read skills/frontend-design/SKILL.md again before touching anything visual, per its own mandate. Two hard redirects flagged in the brief itself and honored: (1) never reproduced the reference's invented vendor stats (28% Higher ROI / 4.6x Faster Growth / 62% Better Targeting / 35% Cost Efficiency) - grepped the whole codebase after finishing to confirm zero matches for that number family or its labels; (2) never used the reference photo itself (real Godrej/Hiranandani developer branding) - sourced entirely new, brand-free imagery instead.
[2026-07-06] Sourced 5 new images, each license-checked live at pull time (Unsplash License, unsplash.com/license) before downloading, same discipline as rounds 3-5: hero (web/public/assets/images/hero-towers-dusk.jpg, Mathias Reding, "panoramic view of a modern city skyline at dusk" - confirmed generic/anonymous, no identifiable city or brand, chosen specifically because the hero represents the whole product, not one city); Mumbai (mumbai-gateway-of-india.jpg, Renzo D'souza, caption "View of the Gateway of India from the sea," location-tagged Mumbai - confirmed genuine, not illustrative); Bangalore (bangalore-vidhana-soudha.jpg, Arif Khan, location-tagged "Vidhana Soudha Layout, Bengaluru" - confirmed genuine); Hyderabad (hyderabad-charminar.jpg, Sunny/@imkss, caption+location "Charminar (Old City), Hyderabad" - confirmed genuine); Chennai (chennai-marina-beach.jpg, Karthick Gislen, location-tagged Chennai, tagged "marina beach chennai" - confirmed genuine, though it's an aerial cityscape near Marina Beach rather than a close-up of the lighthouse specifically, noted honestly in the manifest). Verified each photo's actual subject via WebFetch on its Unsplash page (caption/location tags), not assumed from the search snippet alone - this matters because the previous Pune photo (round 3) turned out to only be search-context-tagged, not caption-confirmed, so this round's 4 new city photos are held to a stricter bar and marked illustrative:false accordingly, unlike Pune which stays illustrative:true. Replaced the pre-existing HOTLINKED hero image in Home.tsx (a live images.unsplash.com URL that predates round 3's no-hotlinking rule and had gone unnoticed until this round's full redesign touched Home.tsx) with the newly downloaded local asset - this closes a real, previously-undetected inconsistency with a rule this project has enforced since round 3.
[2026-07-06] Extended the image manifest's data model to carry the `illustrative` flag through to the UI (previously it lived only in `_meta` for human/DECISIONS.md reference, and parseManifest discarded `_meta` entirely, tested as "should be ignored"). Added `ImageManifest.meta` (parallel per-bucket structure) and `asMetaRecord()` with the identical defensive-normalization philosophy as image paths - a malformed or missing `_meta` degrades to "no tag shown," never a crash. Updated the existing round-5 test suite for the new behavior (10 cases now, was 8) rather than leaving it asserting the old "meta is ignored" contract, since that contract itself changed. `ImageTile` gained a `showIllustrativeTag` prop (opt-in, since the 40px-equivalent-scale tiles have no room for a text badge) rendering a "Representative image" tag on card-sized tiles - satisfies the brief's requirement that illustrative stock never silently pass as the real place.
[2026-07-06] Rebuilt CityView's corridor list from a bar-chart-first grid to ranked reasoning cards (rank badge, photo, ScoreRing, and 2-4 WHY lines) per explicit client instruction that bars don't communicate WHY a corridor ranks where it does. Reason lines are generated only from fields that actually exist per-corridor in the exported JSON (eligible/failed_gates, a live count of that corridor's societies/brokers/candidates, and the freshest candidate's days_since_posted) - deliberately did NOT invent an "X of Y listings signal-matched" ratio the brief's own example suggested, because total-listings-scanned-per-corridor was never exported to the frontend's JSON and fabricating it would violate this project's own no-invented-numbers rule more than it would satisfy the example's literal wording. The round-6 charts (CorridorSignalChart, StalenessHistogram) were not deleted - they're now a "Distribution" toggle view alongside the new default "Ranked" view, so the analytical view survives as instructed. Corridor cards now link directly to that corridor's Society Finder (`/society`), not the CorridorDetail hub, per the brief's explicit "click a corridor -> its societies" - Broker Finder and NRI/Absentee Finder remain one click away via the breadcrumb trail any of the finder pages already show back to CorridorDetail, so nothing became unreachable.
[2026-07-06] Rebuilt SocietyFinder from a data table to an image-first card grid (photo, name, ScoreRing, 1-2 real reason lines: supply depth, then resale velocity or rating if resale velocity is unavailable). Sort control collapsed from 5 sortable table columns to a single dropdown (fit score / supply / velocity / rating) since a card grid has no natural place for 5 clickable column headers - price_consistency_cov dropped from the sort options specifically (still visible inside the source-inspector modal on click, just not a grid sort key) as the one metric least legible as a single number on a compact card.
[2026-07-06] Added a small visual-consistency touch to CorridorDetail.tsx (the 3-tile hub page, otherwise unchanged this round): the corridor's own manifest/placeholder image now sits behind the header as a faded banner, tying it visually to the same corridor image already shown one click earlier on CityView's ranked card - a minimal change, not a redesign of that page, since its 3-tile layout already read as an intentional visual element rather than a bare table.
[2026-07-06] Home.tsx rebuilt to the reference's structural beats (full-bleed hero, headline/subhead, city search + Analyze CTA, 4-step strip, stat tiles, and a closing row) but with real content throughout: headline/subhead describe the product's actual capabilities, StepStrip is unchanged (it already matched the requested 4 steps from round 0). The reference's "recent projects" row has no equivalent concept in this data - built CityRunStatus.tsx instead, a per-city run-status row (corridors tracked, brokers indexed, candidates flagged, fully-scored or not), reusing the same real per-city aggregates already shown elsewhere rather than inventing project rows to fit the reference's literal shape.
[2026-07-06] Data layer expansion (broker/society enrichment + absentee evidence), pre-UI backend work per explicit user instruction. All three additive on top of locked modules, none of which changed: corridor scoring, the four separate scores, broker_meta's phone/company staying NULL, validated_signals.py's detection thresholds (2+ word distinctive names, the 4-keyword never-occupied set).
[2026-07-06] Absentee evidence (item 3): added `evidence: str = ""` to SignalRow (scoring/validated_signals.py), populated from data ALREADY computed in-memory during detection - sibling listing_ids sharing the same normalized owner name for multi_property_owner, the specific matched keyword(s) for never_occupied. Purely additive: verified against the real Pune DB that the same 30/11/8/50/9-per-city candidate counts are unchanged, only the new field is populated. Did NOT re-run recover_detail_pages (would need Playwright + live MagicBricks network access, unnecessary risk for an additive field) - instead added `regenerate_evidence_column()` to run_production_pipeline.py (`--evidence-only` flag) that re-runs find_validated_candidates DB-only and merges the new evidence field onto the already-recovered address_recovered/owner_note in the existing CSVs. Backfilled all 5 cities this way, recombined, re-exported - 108 total rows unchanged, confirmed via row-count match before and after.
[2026-07-06] Broker contact enrichment (item 1): new scoring/broker_contact_enrichment.py + broker_contact_enrichment table, keyed by (city_id, agent_normalized) since the same agency active in two corridors is one real-world business, one lookup. Deliberately reuses the SAME GOOGLE_MAPS_API_KEY already integrated for society ratings rather than adding JustDial/IndiaMART scraping (no prior reachability recon on either site, per this project's repeated 99acres/NoBroker-style caution) or a raw Google search scrape (explicitly barred by the brief) - Places API Text Search + Details is the legitimate-API path the brief itself names as acceptable. Confidence gate (same discipline as sample_detail_url): a Places hit is only accepted if the returned business name shares a significant word with the broker's posted name AND the returned address mentions the city - verified with 4 mocked cases (genuine match, right-name-wrong-city, right-city-wrong-business, zero-results) committed as tests/test_broker_contact_enrichment.py, all reject correctly except the genuine match. Ran build_broker_contact_enrichment.py against the real DB (1119 unique broker/city pairs): degrades cleanly to 0/1119 matched with a clear "GOOGLE_MAPS_API_KEY not set" message, same honest pattern already established for Maps ratings - this environment has no key configured, so nothing is actually populated yet, by design, not a bug.
[2026-07-06] Society detail enrichment (item 2): new scoring/society_enrichment.py + society_detail_enrichment table, keyed by (city_id, corridor, society_normalized), scoped to the same qualifying societies already in society_meta (Society Finder's own set). Interpreted "search by name+city+official" as the Places API's website field on a confidently-matched business listing (same confidence gate and reasoning as broker enrichment) rather than adding a second paid search-API dependency (Google Custom Search/Bing/SerpAPI) the user hasn't chosen or configured - flagged explicitly in the module docstring as a scoping-down assumption, redirect if a dedicated search API is wanted instead. The official page (once found) is fetched exactly ONCE (no crawling), fields extracted via conservative regex: RERA number requires the literal string "RERA" nearby, not just a digit pattern that resembles one (verified this guard with a mocked page containing a RERA-shaped tracking ID with no RERA context - correctly returns None). Sales contact reuses the Places Details phone number already fetched for the confidence check rather than regex-scraping a phone off arbitrary page HTML. Verified extraction against a realistic mocked developer page (unit count, RERA number, possession status, description all correctly pulled) - both cases committed as tests/test_society_enrichment.py. Ran build_society_enrichment.py against the real DB (50 qualifying societies): degrades cleanly to 0/50 matched, same "key not set" message.
[2026-07-06] Wired all three into export_frontend_data.py (additive fields on brokers.json/societies.json/absentee_candidates.json, 2 new stats fields for enrichment coverage) and web/src/types.ts. BrokerFinder gained a "Verified contact" column (phone/website/address + "sourced from" label, or "Unavailable" with the confidence note as a hover tooltip) - the existing activity-list disclaimer banner updated to name this as a separate, confidence-gated step, not a claim about MagicBricks itself. SocietyFinder: clicking the society name (new) or its ScoreRing (existing, now shares one handler) opens the source-inspector panel with the existing 4 society_fit_score metrics AND whatever detail enrichment resolved, with a real link to the official page when one exists - "not found on page" per-field when enrichment matched but a specific field wasn't extractable, "unavailable, no confident match" when Places itself found nothing confident. PipelineLineage's "Signal matched" node now shows the actual evidence text (sibling listing IDs or matched keywords) as its value, with the signal name demoted to the node's caption label - satisfies "render the actual matched detail in place of the vague label" directly, since the vague label was literally what that node showed before.
[2026-07-06] Item 4 (price band for Mumbai/Bangalore/Hyderabad/Chennai): confirmed as explicitly out of scope for this pass per the user's own instruction - no code changed, no price band invented for any of the four cities. Remains a to-do for Mesa (a real, business-confirmed number per city in config/cities/<city>.py, same shape as Pune's).
[2026-07-06] Broker count reconciliation (blocking item, resolved with exact numbers): 1,571 broker_meta rows = 1,118 unique real-world (city, agent_normalized) businesses + 453 "extra" rows contributed by 334 agents active in 2+ corridors within the same city (1,118 + 453 = 1,571, verified by direct SQL, not assumed). Zero placeholder names ("mb user"/"magicbricks user"/"magicbricks"/"real estate") found in broker_meta itself - confirmed they're excluded upstream in broker_list.py's _group_brokers, so they contribute 0 to this gap, not a hidden factor. Found and fixed a real, separate bug while reconciling: build_broker_contact_enrichment.py's _unique_subjects originally did `SELECT DISTINCT city_id, agent_normalized, agent_display`, which double-counted Bangalore's "global edifice infra" (normalized) because two different corridors' broker_meta rows spell it differently - "GLOBAL EDIFICE INFRA" vs "Global Edifice Infra" - inflating the true 1,118 to 1,119 lookup subjects (would have cost one redundant Places API call per such case, though the DB's own PRIMARY KEY on (city_id, agent_normalized) meant the stored result was still correct either way). Fixed by grouping in Python and picking the most common display spelling per (city, agent_normalized), same tie-break broker_list.py already uses within one corridor - re-ran, now exactly 1,118. Added a regression test (tests/test_build_broker_contact_enrichment.py) reproducing the exact same-normalized-different-capitalization shape; proved it's a real regression guard by temporarily reverting to the buggy DISTINCT query, watching the test fail with "expected exactly 1 subject, got 2", then restoring.
[2026-07-06] CLOSED DECISION: no GOOGLE_MAPS_API_KEY will be added for this prototype - broker contact enrichment and society detail enrichment stay built, tested (against mocks - see tests/test_broker_contact_enrichment.py, tests/test_society_enrichment.py, tests/test_build_broker_contact_enrichment.py) and wired into the UI, but permanently un-activated in this environment, not "pending a key later." The cost and account-setup friction of a live Google Cloud billing account weren't worth it for a demo. Both features stay visible and clickable in the UI exactly as already built: Broker Finder's "Verified contact" column shows "Unavailable" for every row with the real confidence_note ("GOOGLE_MAPS_API_KEY not set, lookup skipped") as a hover tooltip; Society Finder's click-through panel keeps the 4 real society_fit_score metrics working exactly as before and shows "unavailable, no confident match" for the enrichment fields, never hidden or removed from the panel. No fallback was added that estimates or derives these fields from other data (no guessing unit count from listing count, no inventing a RERA number shape) - same never-fabricate rule as everywhere else in this project. Revisit only if a specific company or interview explicitly asks for live contact/society detail enrichment - at that point the only change needed is setting the env var, both modules already handle a real key correctly (verified via the mocked confidence-gate tests).
[2026-07-06] Regression pass after the above (spot-checked, not just re-run): absentee evidence confirmed rendering real matched detail (sibling listing IDs / matched keywords) across multiple cities' current absentee_candidates.json output, not the old vague label - see the specific rows checked in this pass. Four scores remain structurally separate (ScoreRing's kind-based coloring untouched, no merging logic added anywhere this round). "Recent Projects" stays removed (CityRunStatus, round 7). StatRow still computed live from data.stats. PlaceholderTile remains the default for any corridor/society/city with no manifest entry (nothing added this round touches ImageTile's fallback logic). LoadingState (animated) unchanged. NotScoredState still gates Society Finder for the 4 unconfigured cities. Broker Finder's and Absentee Finder's activity-list/likelihood-proxy disclaimers both still present, banner copy only extended (round 8), never softened or removed.
[2026-07-06] Received the green/black "intelligence terminal" re-skin reference-image prompt set (5 prompts: landing hero, India network map, corridor tabs+table, 3-tile drill-down, ranked list template) - visual reference only, no build yet, per the user's own instruction to treat generator output as style reference (palette/globe treatment/card look), not literal text to copy. Confirmed one open question before any build starts: the simplified sidebar's "Sectors" nav item is corridors, renamed - not a new grouping layer, not City Explorer renamed. When the actual redesign build lands, "Sectors" in the UI maps 1:1 to the existing corridor data (city -> corridor -> society), only the label changes.
[2026-07-06] Accidental incident recovery: user ran run_production_pipeline.py pune directly (outside this session, on bad advice from another assistant that mistook the scraping entrypoint for a report script), adding 210 rows to Pune's raw_listings (ids 8611-8820, scraped 2026-07-06) on top of the verified 2100-row/871-2970-id production baseline from 2026-07-05. Verified before touching anything: zero cross-references between original and incident rows via duplicate_of_id, zero incident rows had duplicate_of_id set at all (dedup never ran against the mixed set), and society_meta/broker_meta for Pune both last computed hours before the incident - nothing downstream was recomputed against the polluted data. Deleted exactly the 210 rows (city_id='pune' AND id > 2970), verified count back to exactly 2100 and max_id back to 2970 (lowest of all 5 cities' max_ids, consistent with Pune being scraped first). Re-ran find_validated_candidates(pune) read-only post-cleanup: still exactly 30, matching the already-shipped CSV/JSON - confirms the DB is now consistent with what's deployed.
[2026-07-06] Completed the violet/indigo/purple rename properly, following up on the initial values-only re-skin pass. The first pass changed --color-violet-*/--color-indigo-* hex VALUES to green but kept the KEY names, reasoning it would avoid a large diff - a second grep sweep found 47 remaining matches, mostly Tailwind utility classes using shades (violet-300/900/950) never redefined in the theme, which still resolved to Tailwind's actual built-in purple. Fixed properly this time: renamed theme keys to --color-accent-300/400/500/600/900 in index.css (dropped --color-indigo-* entirely, folded into accent since indigo's only uses were 2-stop gradients now flattened to solid per the brief's "single accent, no blending" instruction - Home.tsx's Analyze button and StepStrip's step-1 badge both went from a violet-to-indigo gradient to solid accent-500, with text color corrected from white to black for contrast against the brighter solid fill), then mechanically renamed every call site across 19 component/page files. Re-ran the grep sweep after: 0 matches. Verified the new accent-* utilities actually generate real CSS (not silently-missing classes) by checking the built dist/assets/*.css output directly, not just trusting a clean build. Full re-skin/India-map/transition work (in progress before the incident) resumes next.
[2026-07-06] Resumed the queued motion/transition work (the color rename pass did not build any of it, per explicit correction). Built IndiaNetworkMap.tsx (new route /explore-map): a stylized dot-matrix India silhouette (hand-authored approximate polygon, not a licensed map asset - explicitly illustrative, same honesty standard as image sourcing) via a single SVG <pattern>+<clipPath> rather than ~1000 individually-rendered dot elements, with the 5 registered cities as real HTML-overlay nodes (positions are approximate/stylized, not surveyed). A highlighted city (passed via ?city= query param) renders last in the stagger order, pulses once (not looping), and gets a floating pill label sharing layoutId with its eventual Breadcrumb segment.
[2026-07-06] Built shared-element continuity via a single <LayoutGroup> in App.tsx wrapping all routes: (1) CitySearchBar.tsx extracted as one component with layoutId="search-bar", used by both Home (hero variant) and IndiaNetworkMap (pinned variant) so Framer Motion animates one continuous element between the two pages instead of cross-fading two unrelated ones; (2) Breadcrumb.tsx gained layoutId={`crumb-${label}`} per segment - a label that already existed in the previous page's trail (e.g. the city name, present in every page from CityView onward) shifts smoothly instead of fading out and back in, while a genuinely new segment just fades in, with zero manual "is this new" bookkeeping since Framer Motion resolves it from whether a matching layoutId existed in the previous render; (3) CorridorDetail's 3 tile icons (Society/Broker/NRI-Absentee Finder) share layoutId={`tile-icon-${title}`} with a matching icon added to each destination page's header (SocietyFinder/BrokerFinder/AbsenteeFinder didn't have a header icon before this - added one specifically to have something for the tile to morph into).
[2026-07-06] Built the map -> city "focus pull" transition as a local-state effect rather than true camera/parallax work (flagged as a simplification, not attempted to fake): clicking a node or the "Open City" CTA sets a `focusingOn` state, blurs+fades the dot field, connecting lines, non-target nodes, and the search bar over 500ms, scales the target node up 2.4x, then navigates after a 550ms hold so the effect is actually visible before the route unmounts the page. Fully skipped under reduced motion (direct navigate(), no blur/scale animation queued at all) - checked via useReducedMotion() before the timeout is even set, not just a shorter duration.
[2026-07-06] Fixed a real, pre-existing reduced-motion gap while building this: the route-level page-transition (App.tsx's AnimatedRoutes, x-slide + fade, existing since round 6) was never actually reduced-motion aware - the project's global CSS media query only forces CSS transition/animation durations toward zero, which has no effect on Framer Motion's JS-driven `animate` prop. Added an explicit useReducedMotion() check there too (falls back to a plain opacity fade, no x-slide) while doing this round's work, since the same gap would have undermined the new transitions' own reduced-motion handling if the outer route wrapper still slid regardless.
[2026-07-06] Verified all 19 files touched by the violet-to-accent rename via direct grep of every matched line (not just a passing build) - confirmed correct accent-* class pairings (text/bg/border/shadow/outline) with no broken contrast combinations and zero leftover violet/indigo/purple anywhere. This is a rigorous code-level check, not a pixel-level visual one - no browser driver exists in this environment (established since round 6), so a final visual pass on the running app is still the user's to do.
[2026-07-06] UI round 7 continued, page-by-page pass, Home only (per explicit instruction not to touch City Explorer/CityView/Society/Broker/NRI Finder this round): correcting the prior entry above - a browser driver IS available in this environment (Playwright, already a project dependency for the scraper itself) and was used this round for real: screenshotted hover/no-hover/focus/mid-transition/mobile states, tabbed through the actual keyboard order and confirmed the hero's aria-label and position in it, pressed Enter on the focused hero and confirmed it navigates to /explore-map, and confirmed reduced-motion skips the animation and navigates in ~20ms instead of ~550ms - not just a code-level check this time. Rebuilt Home.tsx: removed StepStrip/StatRow/CityRunStatus/CitySearchBar/the McKinsey citation from this page entirely (components themselves left untouched and unremoved, per the explicit instruction to confirm before deleting anything reusable - grepped first and confirmed all three were Home-only, no other page references them, so nothing else needed touching to make this safe). Hero is now min-h-screen full-bleed (previously a rounded, contained card), the whole hero is one real `<button>` (not a div with synthetic click handling) so Enter/Space activation is native, not manually wired, and reduced-motion is checked before the rotation is even queued (same pattern as IndiaNetworkMap's own focus-pull, matched deliberately: same 550ms duration, so every "click something and watch it settle before navigating" moment in the app now shares one timing convention instead of each page inventing its own). Reverted the headline/subtitle back to the original "Smarter Expansion. Stronger Decisions." copy + its original pairing subtitle (this page had drifted to "Expansion intelligence, built on real data." at some point after round 3 - reusing "its existing subtitle" was read as the subtitle that originally paired with the quoted headline, not whatever currently sits under the drifted one, since the two were never written as a pair). Found and fixed a real bug before calling this done: the "Enter" hover/focus prompt was built as a Framer Motion-animated child of the staggered fadeUp group, which meant Framer's own inline opacity:1 (applied once the page's entrance stagger completes) permanently overrode the Tailwind opacity-0 hover trigger by CSS specificity - confirmed via screenshot with the mouse nowhere near the hero, prompt still showing. Fixed by pulling it out of the Framer stagger entirely into a plain div driven by CSS-only group-hover/group-focus-visible, re-verified with mouse-away and mouse-over screenshots showing the correct hidden/shown states.
[2026-07-06] Hero image (web/public/assets/images/hero-global-network.jpg) is a manual drop-in by the user this round, not sourced/downloaded/license-verified by this session the way every other image in rounds 3-7 was (Unsplash License checked live at pull time in each of those cases). Source, license type, photographer/creator, and pull date are NOT logged here because they are not actually known from this side - logging placeholder values would misrepresent them as verified the way the other entries are. Flagged back to the user directly rather than guessed. File does not exist on disk yet at time of this pass (confirmed via ls, not assumed) - code correctly references the path regardless, and the missing-file state was verified to be a genuine 404 (Vite's dev-server SPA fallback returns a 200 with an HTML body for ANY unmatched path in dev mode, which looked like a false-positive "it's already there" on first check via response status alone - caught by checking content-type/size, not just status code, before trusting it).

[2026-07-09] Note: `web/` now holds a fresh from-scratch hero+sphere+map rebuild (React Three Fiber + GSAP), unrelated to the multi-page dashboard SPA the "Frontend (web/)" entries above describe - that app was moved to `archive/old-ui/` before this rebuild started. Its own Phase 1 (revolving sphere hero) and Phase 2 (click-transition + India map) work predates this entry and wasn't logged here at the time; picking the log up from this point forward. See PROGRESS.md for current status.
[2026-07-09] Lesson: SphereGeometry's UV seam (u=0 meets u=1, unavoidable when a non-panoramic photo is mapped onto a fully-rotating sphere) combined with default mipmapping caused a dark flash sweeping through the visible horizon once per rotation - triangles either side of the seam have near-opposite UV derivatives, and mipmap LOD selection uses those derivatives to pick a level, getting it wrong right at the seam. Fixed by disabling mipmaps on that texture (`generateMipmaps=false`, `minFilter=LinearFilter`) - removes the LOD-selection artifact specifically; the seam itself (a content discontinuity, not a flash) can't be removed without a seamless source image, and is reduced to a much milder, non-flashing cosmetic line. Could not visually 100%-confirm the original flash in this environment's software-rendered (SwiftShader) WebGL despite a dense-rotation sweep and an exhaustive fixed-angle scan - real GPU driver behavior for mip-selection artifacts can differ from software rendering, so this is flagged as reasoned-and-tested but not visually reproduced, not a fully closed loop.
[2026-07-09] Lesson: a Fresnel rim-light on a close-up "horizon view" sphere (camera near a huge sphere, seeing only its cap, not the whole ball) reads far thicker/softer on screen than the identical shader on a small distant sphere, because a wide swath of the visible surface near the silhouette is already close to grazing - the fresnel value itself changes slowly across screen space there. Getting a genuinely thin line needed both a much higher power exponent (26 vs. an initial 5) and a lower intensity multiplier (so the falloff doesn't clip a wide swath to full brightness) - power alone wasn't enough while intensity stayed high.
[2026-07-09] Decision: replaced that uniform rim shell with individual radiating "rays" (HeroSphere.tsx) per explicit brief - distinct lines fading outward from the horizon, not one continuous glowing ring. Ray base points are computed analytically from the sphere's true silhouette circle (standard tangent-cone geometry from camera position + sphere radius), not sampled or approximated, so it stays exact if camera/sphere position ever changes. Camera and sphere are both static relative to each other during idle (only the mesh's own texture spins), so this computes once on mount via useMemo, not per frame.
[2026-07-09] Lesson: the hero sphere's vh-based container height meant its actual pixel clearance from the hero copy varied a lot with viewport HEIGHT specifically, not just width/breakpoint - it overlapped the subtitle by ~11px at 1366x768 (a very common laptop size) while having 125px+ of clearance at 1920x1080, despite both being the same "lg" Tailwind breakpoint. Fix required checking actual measured clearance at multiple real viewport heights (1366x768, 1280x800), not just the primary dev-viewport size, before and after repositioning.
[2026-07-09] Fixed IndiaMap.tsx's silhouette: the hand-authored polygon reused from archive/old-ui (itself already labeled "illustrative, not surveyed cartography") turned out to be a genuinely poor approximation once compared directly against this project's own india.png reference - no northeast extension, a rounded blob instead of the real peninsula taper. Rather than hand-guess a better polygon from memory (same risk of being just as crude), traced it from that same reference image instead: Gaussian blur (radius 14) to merge its dot-matrix texture into a solid fillable silhouette, kept only the largest connected component + filled small internal holes (drops the reference's stray decorative "ocean" nodes and the notches its own city-label boxes punch into the landmass), marching-squares contour (skimage.measure.find_contours) simplified via Douglas-Peucker (skimage.measure.approximate_polygon) to 99 points. The reference's own 5 city marker dots were identified the same way - the only blobs in a white/bright color-threshold pass with a consistent ~90px size, distinct from the much smaller, irregular label-text glyphs - and mapped through the identical coordinate transform so markers land correctly against the new outline. Still hand-derived from a stylized reference image, not authoritative GIS data, but now actually recognizable as India. Side effect caught by testing, not assumed away: the new, more geographically accurate positions place Mumbai/Pune and Bangalore/Chennai closer together than the old rough coordinates did, which overlapped their labels at the 390px mobile width - fixed with a smaller mobile-only label size/padding (text-[9px] vs sm:text-xs), verified clean at that viewport afterward.
[2026-07-09] Removed HeroSphere.tsx's rim-light entirely (both the original solid Fresnel ring and its later ray-burst replacement) per explicit instruction to revert to a bare, minimal rotating sphere with no glow effect - confirmed via a repo-wide search that no genuinely particle-constructed sphere ever existed in this project's history (no git history exists to check directly, but archive/old-ui has no 3D/Three.js code at all, and no other copies of HeroSphere.tsx exist anywhere), so "the original rotating particle sphere" in that request was read as the sphere+ParticleField combination described loosely, not a distinct past implementation - stated as an assumption rather than blocking on it, since it was cheaply reversible either way. Kept the independently-fixed position (GROUP_Y=-4.35) and particle-texture sharpness from the immediately preceding round, since those were separately-flagged real bugs the user hadn't objected to, not part of "the ring."
[2026-07-09] Scaled IndiaMap.tsx to fill ~92% of viewport height or width (whichever is tighter) via `aspect-ratio` + `h-[92vh]` + `max-w-[92vw]` together, rather than a fixed max-width - found and fixed a real bug in the process: the viewBox was still 400x520 (a portrait ratio inherited from the old hand-drawn path) while the newly-traced outline's actual bounding box is landscape (~1.21:1), so enlarging the container just added proportionally more empty black space above/below the shape instead of making the visible map bigger - caught by comparing rendered output against expectation, not assumed correct after the CSS change alone. Fixed by refitting the viewBox to 400x358 (the traced shape's own aspect ratio) and re-normalizing the path/marker coordinates into it. Known, unavoidable consequence of the shape's fixed landscape aspect ratio: on tall/narrow mobile viewports, width becomes the limiting constraint, leaving vertical letterboxing rather than filling the full height - the alternative (forcing it taller) would require either clipping the shape or distorting its aspect ratio, both explicitly worse than the current behavior.
[2026-07-09] Lesson: an 8-tap in-shader blur (sampling 8 neighboring UVs per fragment, every frame, to fake a bloom effect on the hero sphere's surface) measured 15-17fps in this environment, down from a 33-43fps baseline - the redundant per-fragment texture fetches were themselves the cost, independent of any render-to-texture pipeline. Switched to pre-blurring the texture once at mount (draw the loaded image small onto a canvas, let the GPU's own bilinear upscaling blur it for free at display time) and blending that second texture in - drops to 2 texture reads per fragment, confirmed 48-51fps. General takeaway for this sandbox: per-fragment texture fetch COUNT matters on its own here, not just whether there's a separate render pass - cheap-looking multi-sample shader tricks aren't free the way they'd be on real GPU hardware.
[2026-07-09] Built Phase 3 (CityScreen.tsx + SocietyTab/BrokerTab/NriTab.tsx): horizontal hero placeholder + city name + 3 tabs, reading societies.json/brokers.json/absentee_candidates.json+nri_directory.json filtered by city_id, real fields only. Checked live before building rather than assumed: `rating_available`/`detail_available` are false for all 128 societies and `contact_available` is false for all 1571 brokers right now (no GOOGLE_MAPS_API_KEY set upstream) - rather than rendering 5-6 "Unavailable" fields per card, each tab shows one summary note instead. `absentee_candidates.json`'s `score` field is a string ("2"), not a number, in the source data - preserved as-is (displayed, not coerced/sorted as numeric). `nri_directory.json` has no `city_id`, only a `city_focus` free-text field that's blank for 71% of rows and inconsistent (typos, non-target-city values) for the rest, with zero rows for Pune or Chennai - per explicit user decision, the full directory always renders with city-matching rows (fuzzy-matched against the specific typos actually observed: "Banglore," "Blr") sorted first, not filtered to an empty state for the two cities with no tagged rows.
[2026-07-09] Restructured Phase 3: city -> flat 3 tabs was wrong, doesn't match how the backend actually scores things (societies/brokers/absentee signals are grouped and ranked WITHIN a corridor, never city-wide - see scoring/corridor.py, society_fit.py, broker_list.py). Corrected flow: city -> CorridorList.tsx (ranked, corridors.json) -> CorridorDetail.tsx (renamed from CityScreen.tsx, now takes both cityId and corridor) -> the same 3 tabs, now filtered by corridor too. Verified before building, not assumed: 100% of corridor name strings in societies.json/brokers.json/absentee_candidates.json match a corridor.json entry for their city, across all 5 cities - no export change was needed for this join. `nri_directory.json` has no corridor field at all (confirmed already, prior entry) - its directory section stays explicitly labeled as city-wide, not corridor-scoped, inside the corridor screen, rather than silently implying corridor-level relevance it doesn't have.

Major finding surfaced to the user before building the "why this ranked" panel: corridors.json's entire ranking is hand-seeded placeholder data - confirmed directly in every config/cities/*.py docstring ("SEED / PLACEHOLDER values, not yet computed from live data"), for ALL 5 cities including Pune, not just the newer/unscraped ones. Separately, config/cities/*.py's `canonical_societies` field (used only for the placeholder corridor-candidate seeding) is ALSO literal placeholder text ("Whitefield Society Seed 01" etc.) for 4 of 5 cities - a DIFFERENT, unrelated real production pipeline (raw_listings -> society_meta -> societies.json) is what actually backs the real society/broker/absentee data shown in the UI. Two entirely separate data paths sharing a similar shape; the seed-data path only feeds corridors.json.

Per explicit approval, two export_frontend_data.py changes: (1) `export_corridors()` now also includes `raw_metrics` (the actual CorridorCandidate values - km, comp count, DOM days, txn count) alongside the existing normalized `sub_scores`, so the explanation panel can show real numbers for all 5 gates, not just the 2 that happened to already be raw percentages. (2) new `_exclusive_society_sample_urls()` (export_frontend_data.py), a society-scoped adaptation of the existing broker `_exclusive_sample_urls` - same "URL provably belongs to only this one" guarantee, grouped by (corridor, society_normalized) via `society_name_raw`/`normalize_society_name` instead of agent. Deliberately NOT filtered to in-band-only listings the way scoring/society_fit.py's own grouping is, since most cities have no configured price band at all and that would zero out coverage almost everywhere - this is traceability (one verifiable example), not score reproduction. Coverage after adding it: **100% of societies (128/128)** got a sample_detail_url, vs. brokers' existing 35.5% (558/1571) and absentee candidates' 50.9% (55/108) - all real, measured coverage rates, not assumed. Per explicit instruction, society sample links are labeled "Sample listing" (with a title tooltip spelling out it's one example, not full evidence) rather than anything implying complete proof of the score.

Fixed a latent bug found while touching BrokerTab.tsx for the corridor filter: `sample_detail_url` was typed as always-present (`string`) when it's actually null 64.5% of the time - agent names rendered as normal-looking links even with no real href underneath. Fixed by typing it `string | null` and only rendering as a link when present, plain text otherwise - matches how NriTab already handled `traceable`/`detail_url`.
[2026-07-09] Logo (NetworkMark) is now a real button, global "return to landing page" action from any screen - resets every piece of App-level state (selectedCityId/Corridor, isExploring, hovered, converge signal/point), not just `screen`, so a later visit starts fresh. Found and fixed a real, reproducible keyboard-focus bug while wiring this up, not assumed away: after an AnimatePresence screen swap, Chromium's Tab-from-nothing fallback landed on the new screen's first focusable element directly (e.g. a map marker), skipping the fixed nav's logo/Log-in entirely on the very next forward Tab - reproducible via keyboard-only AND mouse-driven transitions alike (so not about click position), reachable only via two Shift+Tabs backward. Fixed with an explicit focus-management callback ref, one per AnimatePresence screen wrapper (tabIndex=-1, invisible landmark, not a new stop in the normal tab sequence) - deliberately a callback ref rather than a useRef+useEffect keyed on `screen`, since with `mode="wait"` the new screen doesn't actually mount until the old one's exit animation finishes, after the state update itself; an effect keyed on `screen` fired before there was anything to focus. Skipped on the very first mount specifically, since a genuinely fresh page load already tabbed correctly (verified) and forcing focus there too would have traded that correct behavior for the same bug on first load instead of on navigation. Note: this still doesn't make the logo the literal first forward-Tab stop after a transition (Tab still proceeds into the new screen's own content, matching standard SPA route-announcement practice) - the logo itself is confirmed reachable and Enter/Space-activatable via direct focus, which is the literal bar the brief set, not "first tab stop."
[2026-07-09] Added per-city landmark background images to CorridorList.tsx: `web/public/images/cities/<city_id>.jpg` (city_id matching cities.json exactly), full-bleed behind the content layer, opacity-90 + saturate(0.55)/brightness(0.8) filter + a dark gradient scrim on top so it reads as atmosphere rather than a pasted photo, graceful onError fallback to plain black if a file's ever missing. Corridor cards gained a bg-black/50 backdrop-blur-sm glass treatment (matching IndiaMap's existing city-label pattern) so they stay crisply legible over a now-busier background. Sourced all 5 from this project's OWN prior, already-verified-licensed images (archive/old-ui/public/assets/images/*, Unsplash License, checked live 2026-07-06 per that round's own manifest) rather than pulling anything new from the web - copied and renamed into the new convention, not re-sourced. 4 of 5 (Mumbai/Gateway of India, Bangalore/Vidhana Soudha, Hyderabad/Charminar, Chennai/Marina Beach) are confirmed-genuine landmark photos per that manifest's own verification notes; Pune's is explicitly flagged there as illustrative-only (generic aerial urban stock, never independently confirmed to depict Pune specifically) - carried that caveat forward rather than presenting it as equally verified.
[2026-07-09] Built SelectionMetrics.tsx (India map screen, `IndiaMap.tsx`) per explicit instruction to confirm the metric list before displaying anything. Read all 4 scoring modules directly rather than assuming: `gate_config.py` (5 corridor gates: price-band fit, demand-hub proximity, clean title/RERA, comp density, resale velocity - currently 100% hand-seeded placeholder data per city config docstrings, already-documented elsewhere), `society_fit_config.py` (4 weighted metrics: supply depth 0.30, resale velocity 0.30, price consistency 0.20, rating 0.20 - real/live data), `broker_list.py` (1 metric, listing activity, percentile-ranked - real/live data), `validated_signals.py` (2 rule-based detections, NOT a weighted score: multi-property owner, never-occupied language). Surfaced a real conflict before building anything: `gate_config.py`'s own docstring states "backend-only (CLAUDE.md hard constraint: metric definitions and weights never reach the frontend)," tracing to this project's original PRD output-layer spec ("No weights or metric definitions in the payload, scores and rankings only"). Flagged directly to the user rather than silently building around it or silently complying with the older constraint. Resolved via AskUserQuestion: show metric NAMES only, never weights/percentages/thresholds (a partial, explicit exception to the constraint, not a repeal of it - the actual numbers still never reach the frontend), covering all 4 categories including NRI signals, visually distinguished as "(rule-based)" rather than presented as a 5th weighted score it isn't.
[2026-07-09] Found and fixed a real mobile-overlap bug in SelectionMetrics before reporting it done: an always-open panel (initial build) covered the map's own Mumbai/Pune/Hyderabad city markers at 390px viewport width, confirmed via Playwright screenshot, not just reasoned about - directly the "must not interfere with the UI" failure mode the original brief ruled out. Rebuilt as collapsed-by-default with a small labeled toggle chip ("Selection Metrics" + info icon, not an icon-only affordance), reusing the exact interaction language already shipped and approved for `ExplainPanel.tsx` (corridor/corridor-list screens) for consistency rather than inventing a second pattern. Verified clean (zero console/page errors) across 1600x900, 1366x768, and 390x844 in both collapsed and expanded states - collapsed never overlaps anything at any tested size; expanded is now an explicit, user-initiated overlay with a visible close (X) control, the same on-demand tradeoff ExplainPanel already makes, not a silent content block.
